# Plasma core — hand-picked, every entry justified (no plasma-meta).
# Deliberately absent: discover (pacman/yay instead), plasma-firewall,
# plasma-thunderbolt, accessibility extras. Add back only when missed.
# okular rejected — PDFs open in the browser (mimeapps: brave), documents in libreoffice.
plasma-desktop
plasma-pa
plasma-nm
powerdevil
kscreen
bluedevil
breeze-gtk
kde-gtk-config
xdg-desktop-portal-kde
kwalletmanager
# kwallet-pam: /usr/lib/pam.d/plasmalogin already calls pam_kwallet5, but as an
# optional module — silently skipped while the package is absent. Installing it
# is the whole fix: the wallet is created with (and unlocked by) the login
# password at PLM login. Without it, a fresh install greets ulu with the
# "Blowfish or GPG?" wallet-creation dialog on first login. Requires no
# autologin (PAM has no password to pass on in that case).
kwallet-pam
plasma-login-manager

# KDE apps — one review round each (docs/LOG.md)
dolphin               # file manager; KIO integration, config in ~/.config/dolphinrc
konsole               # terminal emulator; KDE theming + dolphin F4 integration
ark                   # archive GUI in dolphin context menu — ulu: ALL formats, never the terminal
p7zip                 # ark backend: 7z
unrar                 # ark backend: rar (proprietary format, only reliable extractor)
spectacle             # screenshots/recording; the reliable path under Wayland
gwenview              # image viewer; the "double-click a picture" default
plasma-systemmonitor  # GUI task manager; only Qt-native option (GTK avoided per ulu)
isoimagewriter        # ISO → USB stick; distro-hopper essential, Qt-native
kcalc                 # calculator, Qt-native. Was ALREADY installed here explicitly and required by
                      # nothing, yet stood in no package list — a reinstall would have lost it in
                      # silence. Found 2026-07-31 while adding it on request.
kdeconnect            # phone integration: notifications, clipboard, file transfer, remote input.
                      # ulu asked for it as "kconnect"; no such package exists, the name is kdeconnect.
                      # Nothing to open in nftables: our table has NO input chain at all (only the
                      # output/postrouting pair for the VPN split), so its 1714-1764 range is
                      # unfiltered — verified, not assumed.
                      # Optional dep python-nautilus skipped on purpose: GTK, and ulu avoids it.
                      # Pairing with the phone is manual — see STATUS post-install steps.
