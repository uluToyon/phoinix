# ulu's applications — one review round each (docs/LOG.md).
steam                 # obviously ("denk"). Library re-attach from /mnt/Games is a stage-3 checklist item
discord               # official client; ulu uses voice+chat only — vesktop is the documented plan B for screenshare woes
qbittorrent           # torrent client; Qt-native, config in ~/.config/qBittorrent (paths touch the mount-path topic)
strawberry            # music player; HARD requirement: stereo→5.1 upmix ONLY for music (player-internal,
                      # never system-wide — games/movies keep native channels). Dated layout accepted.
noto-fonts-cjk        # CJK glyphs — without it Japanese track titles in Strawberry render as boxes.
                      # noto-fonts covers Latin/Greek/Cyrillic only; CJK is a separate 299 MB package.
                      # Consumer is Strawberry today, but it fixes CJK everywhere (browser, file names).
# mpc-qt moved to aur.txt — not in official repos (learned during stage 3)
keepassxc             # password vault ("denk"); database lives on FilesMusic and survives reinstalls
# networkmanager-openvpn removed 2026-07-31 — ProtonVPN now runs on WireGuard,
# which NetworkManager speaks natively. The plugin (and openvpn with it) bought
# nothing except an import that could never be unattended: Proton's OpenVPN
# profiles need a separate username and password, a WireGuard config carries its
# own key. The official Proton app stays rejected (GTK).
# libnatpmp removed 2026-07-31 with the port-forwarding service (ulu's call).
# Proton's port forwarding needs a P2P server, and both servers in use refuse
# NAT-PMP; the renewal service was the only consumer. Reviving it means adding
# this back — and enabling qBittorrent's WebUI, which is how the port gets in.
nftables              # the kernel-side guarantee that qBittorrent can only ever leave through the
                      # tunnel. Explicit BECAUSE it was already installed as someone else's dependency
                      # and therefore one `pacman -Rns` away from vanishing (same lesson as socat)
cups                  # printing daemon; printer: Samsung SCX-4300 (USB), print only — no scan stack
# splix moved to aur.txt — dropped from official repos (learned during stage 3)
print-manager         # KDE printing UI (settings + job viewer)
libreoffice-fresh     # office suite (documents; PDFs stay in the browser); fresh = the rolling-release-fitting branch
hunspell-de           # spellcheck dictionary German (documents are German, UI is en_US)
hunspell-en_us        # spellcheck dictionary English
