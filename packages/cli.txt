# CLI tools — one review round each (docs/LOG.md).
bat                   # cat with syntax highlighting; alias cat->bat in dotfiles (as on old system)
eza                   # modern ls; old zshrc aliases ls/ll/la/lt restored via dotfiles (needs nerd font for icons)
zoxide                # learning cd; alias cd->z as on old system
fzf                   # fuzzy finder: Ctrl+R history search, Ctrl+T files; wired up in dotfiles
fastfetch             # system info card; the hopper's first command after every install
btop                  # terminal monitor; replaces htop/glances/s-tui (GPU view incl.)
# expac rejected — orphaned leftover, no active consumer
# uv rejected — only user was the old DZGUI setup; DZGUI 7 is turnkey (bundled runtime)
# wmctrl rejected — X11 tool, suspected DZGUI leftover; no consumer on Wayland
jq                    # JSON processor; consumer: stage-3 DZGUI fetch parses the GitHub releases API
power-profiles-daemon # Plasma power profile switcher (powerdevil backend); enable service in stage 3
base-devel            # build toolchain for AUR packages (paru bootstrap + everything in aur.txt)
# linux-zen-headers rejected — zero DKMS candidates on this system (AMD GPU + MediaTek
# wifi are in-kernel); any future DKMS package pulls headers automatically
ttf-meslo-nerd        # MesloLGS Nerd Font: p10k prompt glyphs + eza icons (konsole default font)
reflector             # re-sort /etc/pacman.d/mirrorlist by hand later. Stage 1 already sorts the
                      # list on the ISO (pacstrap copies it into the target), so this is only for
                      # refreshing it on a long-lived install. Its TIMER is deliberately not
                      # enabled: an unattended weekly re-sort can also pick WORSE mirrors, and a
                      # reinstall regenerates the list anyway.
ddcutil               # DDC/CI over i2c — drives scripts/monitor-switch.sh, which flips all three
                      # monitors between this machine and the laptop. Brings its own prerequisites:
                      # modules-load.d loads i2c-dev and its udev rule tags the graphics i2c devices
                      # with uaccess, so NO i2c group membership and no rule of ours is needed.
# cpupower rejected — redundant next to gamemode (auto) + power-profiles-daemon (KDE click)
